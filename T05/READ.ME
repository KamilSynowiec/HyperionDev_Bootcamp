seven11 - it's a program that determines if a number input by the user is divisible by 7 and 11, either 7 or 11 or neither.

quadrilaterals - the program takes as input each side and the angle of each corner and determines whether 
the shape is square, a rectangle, a rhombus or a parallelogram.
